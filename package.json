{
  "name": "x-factory",
  "version": "0.1.0",
  "type": "module",
  "description": "Software engineering workbench: ticket → Pi → implementation → tests → PR",
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  },
  "engines": {
    "bun": ">=1.2.3"
  },
  "private": true
}
